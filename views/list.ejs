<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
</head>
<body>
    <%- include('navbar.html') %>

    <ul>
        <% for (let i = 0; i < posts.length; i++) { %>
        <li>
            <div>
                <p>글 번호: <%= posts[i]._id %></p>
                <h4><a href="<%= '/list/' + posts[i]._id %>">제목: <%= posts[i].title %></a></h4>
                <p>상세: <%= posts[i].description %></p>
                <p>시작날짜: <%= posts[i].startDate %></p>
                <p>종료날짜: <%= posts[i].endDate %></p>
            </div>
            <button class="delete" data-id="<%= posts[i]._id %>">삭제</button>
        </li>
        <% } %>
    </ul>

    <script src="https://code.jquery.com/jquery-3.6.1.js" integrity="sha256-3zlB5s2uwoUzrXK3BT7AX3FyvojsraNFxCc2vC/7pNI=" crossorigin="anonymous"></script>
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Ga'gi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
    <script>
        $('.delete').click(e => {
            $.ajax({
                method: 'DELETE',
                url: '/delete/' + e.target.dataset.id,
            }).done(result => {
                $(e.target).parent('li').remove();
            }).fail((xhr, status, err) => {
                console.error(xhr, status, err);
            });
        });
    </script>
</body>
</html>